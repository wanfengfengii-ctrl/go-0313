# 交通枢纽金属屋面虹吸排水临时溢流导排拆除闭环

## 项目目标

建设一个可从零生成、可持久化恢复的 Go HTTP 后端，以“汇水分区—雨水斗—有向管段—焊口代次”只追加图为核心，管理高密度聚乙烯管材身份、端口连接、热熔阶段证据、设备租约、异常传播、分区蓄水排放试验、双人复核和唯一终局。采用至少 4 个业务包、20 个生产 Go 文件，生产代码控制在 2000—3000 有效行且目标约 2500 行；使用本地事务型持久化实现原子提交、幂等操作、逻辑时钟和重启恢复。公开测试使用固定虚构交通枢纽数据、同步屏障和脚本化设备结果，不依赖真实时间、随机值或外部服务。仓库必须包含 go.mod、可运行服务入口、迁移或初始化逻辑，以及支持 linux/amd64 与 linux/arm64 的 Docker 构建配置。

## 端到端业务流程

1. 创建施工任务并提交设计、材料、工艺、检测和试验摘要；系统校验摘要版本、整数尺寸及坐标后，一次性锁定汇水分区、雨水斗、管段、端口拓扑、材料批次、支吊架、固定节点、检测映射、蓄水程序、阈值和任务代次。
2. 按操作号原子领用管材或管件、裁切整数毫米长度、生成短节和损耗谱系，同时绑定待焊端口并获取焊机、夹具、刨削器及作业区限时租约；任何身份、端口、库存或租约冲突使整个事务回滚。
3. 热熔焊口依次记录切平、刨削、清洁、对中、加热翻边、切换、加压、保压和冷却证据；规则引擎验证环境窗口、夹具、翻边、切换时限、压力曲线、冷却时限和逻辑时间，只有连续有效前缀能够推进当前代次。
4. 完成外观、内窥、支吊架与固定节点检查后锁定安装证据；设备拒绝、断连、超时、校准过期或格式错误只产生具有确定重试序号和逻辑时间的待重试调用，不产生读数或阶段进展。
5. 按汇水分区执行蓄水、保水、排放和排空试验，使用定点整数计算管内容积、蓄水量、压力积分、排空流量及时间边界；水位计或流量计失败时保持试验屏障。
6. 发现错径、污染、对中或翻边异常、切换超时、压力缺口、冷却扰动、内壁缩颈、渗漏、支架位移或排水不足时，按水力连通、共享焊机加热板污染窗口、材料批次和共同悬吊支路生成排序稳定且唯一的割除重接与复验集合。重接创建新代次，登记割除段去向并重新覆盖相关焊口、管段及水位测点，旧代次和旧证据永久保留。
7. 所有整改和复验闭合后，由两名不同且资质有效的人员独立复核；单写终局屏障在准入、渗漏风险隔离和取消的并发请求间只接受一个结果，准入时生成唯一的临时溢流导排拆除凭据。

## 核心组件与职责

1. 虹吸管网与热熔工艺规则目录：保存带版本的设计及工艺摘要，校验整数几何、水力方向、管径过渡、无环性、唯一可达路径、热熔程序和检测阈值，并为锁定任务提供不可变规则快照。
2. 施工任务及水力拓扑聚合：维护任务代次、汇水分区、雨水斗、有向管段、端口和焊口代次的只追加连接图，执行端口唯一归属、拓扑锁定、重接换代和迟到回执隔离。
3. 材料谱系与资源租约管理器：管理管材、管件、短节、割除段、试样、有效安装段和损耗的唯一身份及整数毫米守恒；以事务方式完成裁切、转换、端口绑定及焊机、夹具、刨削器、作业区、内窥通道和蓄水分区的限时互斥租约。
4. 热熔轨迹及安装证据记录器：实现逐焊口阶段状态机，记录环境、翻边、压力、冷却、外观、内窥、支架和固定节点证据；通过设备适配接口保存成功读数或确定性待重试调用。
5. 蓄水复验与终局仲裁器：执行定点水力计算、蓄水排放时间轴校验、异常复验集合传播、双人复核以及准入、隔离、取消的单写终局裁定。
6. Go HTTP API：提供 JSON 命令与查询端点、稳定错误协议、操作号幂等控制、事务边界、排序确定的拒绝原因、健康检查及重启恢复入口。

## 领域规则与不变量

1. 任务锁定后，其汇水分区、雨水斗、管段全集、端口图、材料批次、支吊架、固定节点、热熔程序、检测映射、蓄水程序、阈值和代次均不可原位修改；设计或工艺摘要版本陈旧时拒绝锁定。
2. 每个管件端口至多属于一个当前有效焊口。锁定图必须无环，且每个雨水斗沿锁定流向存在唯一、连续且合法管径过渡的出户路径；缺口、分叉、逆向、断路、环路和非法突径均拒绝。
3. 所有长度、管径、壁厚、坡度比例、坐标、温度、压力、时间、水位和流量使用带单位的整数或定点整数；明确半向上舍入规则，并在负值、退化尺寸、除零及加减乘累计溢出前返回稳定错误。
4. 材料转换形成只追加谱系边。每个父身份的原始整数毫米长度必须等于仍可用余料、短节、试样、割除段、有效安装段和合理损耗之和；同一身份不得并发重复领用或进入两个有效去向。
5. 焊接阶段只能形成切平至冷却的连续前缀。重复端口、越序阶段、错误夹具或焊机、过期租约、环境窗口过期、加热板跨批污染、逻辑时间倒退、错误代次及冷却期扰动均不得写入有效阶段证据。
6. 设备失败只追加调用尝试和待重试状态；重试次数由任务快照确定。无有效读数时不得推进焊接或蓄水阶段、释放租约屏障、完成复验或签发准入。
7. 复验集合以异常标识和任务代次派生唯一键，并取当前水力连通范围、污染窗口、同材料批次和共同悬吊支路影响范围的确定并集；输出按建筑区、汇水分区、雨水斗、管段、端口、焊口、代次排序。旧代次迟到回执仅作为审计事件保存。
8. 相同操作号和规范化内容的重试返回原结果；相同操作号内容不同返回幂等冲突。所有命令以单事务提交，失败不得遗留身份绑定、谱系边、租约、有效阶段、蓄水证据或准入凭据；终局一经写入不可被其他终局覆盖。

## 数据模型与持久化

1. RuleSnapshot：摘要版本、建筑区、分区与雨水斗定义、管段几何、允许管径过渡、热熔阶段参数、环境窗口、压力及冷却阈值、检测与蓄水程序。
2. TaskAggregate：任务 ID、任务代次、锁定状态、逻辑时钟、规则快照 ID、终局屏障版本及只追加事件序号。
3. HydraulicGraph：Zone、RoofDrain、PipeSegment、Fitting、Port、Outlet、SupportBranch、Hanger、FixedNode、DirectedEdge，以及当前有效 WeldGeneration 引用。
4. MaterialLineage：MaterialIdentity、Batch、MaterialKind、LengthMM、ParentIdentity、Disposition、LineageEdge 和守恒快照；去向覆盖余料、短节、试样、割除段、安装段与损耗。
5. Lease：资源类型、资源 ID、持有人、任务及焊口代次、逻辑起止时间和释放原因；同资源的有效时间区间不得重叠。
6. WeldEvidence：焊口 ID、代次、两端口、焊机与夹具、阶段前缀、温度、翻边、切换、压力点列及积分、冷却记录、外观与内窥结果、有效性和替代关系。
7. TestAndReview：分区蓄水会话、水位及流量读数、排空结果、异常、复验集合、两名复核人员及独立签署、终局类型和唯一准入凭据。
8. OperationRecord：操作号、规范化请求摘要、响应或稳定错误码、事务提交序号；DeviceAttempt 保存设备类型、脚本调用键、逻辑时间、尝试次数、结果分类和可重试状态。持久层采用事务型嵌入式数据库，并在重启时从快照与只追加事件恢复聚合。

## 公开接口

1. POST /v1/tasks 与 POST /v1/tasks/{id}/lock：创建并锁定任务；锁定失败返回版本、几何、拓扑或材料校验原因。
2. POST /v1/tasks/{id}/materials/operations 与 /leases：原子执行领用、裁切、谱系转换、端口绑定和资源租约获取或续期。
3. POST /v1/tasks/{id}/welds/{weld}/generations/{generation}/stages：提交热熔阶段或设备调用结果；POST .../inspections 提交外观、内窥和安装证据。
4. POST /v1/tasks/{id}/zones/{zone}/water-tests：创建并推进蓄水、保水、排放及排空试验；设备结果通过脚本化适配器关联调用键。
5. POST /v1/tasks/{id}/anomalies 与 /repairs：登记异常、查询确定复验集合、割除旧焊口并创建重接代次；GET 查询保留的历代证据。
6. POST /v1/tasks/{id}/reviews 与 /final-decisions：提交独立复核并竞争准入、隔离或取消终局；所有写接口要求 Operation-Id，错误响应包含稳定 code、按规定字段排序的 reasons 和原事务版本。

## 失败边界

1. 目录与锁定边界：摘要陈旧、端口重复、拓扑成环或断路、非法突径、退化尺寸和定点溢出在聚合写入前失败。
2. 材料与租约事务边界：并发领用、谱系不守恒、端口占用或租约竞争会整体回滚，不得只完成其中一项。
3. 焊接证据边界：越序、错误设备、污染窗口、过期租约、乱序时间、切换或冷却超界，以及设备拒绝或超时，不得改变有效阶段前缀。
4. 复验换代边界：重接代次冲突或迟到旧代次回执不得覆盖当前结论；创建新代次、割除去向、谱系变化和复验范围必须原子提交。
5. 蓄水与终局边界：无读数、校准过期、排水不足或复验未闭合时保持屏障；并发准入、隔离和取消使用持久化比较交换，只能提交一个终局。
6. 持久化与恢复边界：进程在事务提交前中断时不得留下部分状态；提交后重启必须恢复相同聚合版本、租约时限、幂等响应、设备重试序号和终局。

## 验收标准

1. 锁定命令固定全部施工、拓扑、材料、工艺、检测、蓄水和任务代次数据；陈旧摘要或重复锁定内容冲突以稳定错误拒绝。
2. 公开整数坐标管网通过无环、端口唯一和雨水斗至出户口唯一可达校验；环路、断路、非法管径变化、负数、退化值、除零与乘加溢出均可确定复现。
3. 并发裁切、材料转换、端口绑定及租约获取保持唯一身份和整数毫米守恒；相同操作幂等，不同内容冲突，任一失败经重启仍无部分副作用。
4. 每个焊口只接受锁定的连续热熔阶段前缀；切换与冷却边界、压力积分、污染窗口、租约过期、逻辑时间乱序及脚本化设备故障均产生精确稳定结果。
5. 异常能够一次性生成排序确定的割除重接与复验集合；新代次成为当前结论来源，旧焊口、割除短节、谱系和检测证据永久可查询且迟到回执不改变结论。
6. 蓄水量、管内容积、排空流量和时间轴使用确定定点运算；水位或流量设备失败、支架位移、渗漏或排水不足不得越过复验屏障。
7. 只有全部守恒、拓扑、焊接、安装、蓄水、复验和两名不同合格人员复核条件满足时才能签发唯一准入凭据；并发准入、隔离、取消最终仅有一个持久终局。
8. 项目可通过 go test ./...，公开测试分布在至少 4 个测试文件且不少于 12 个独立用例；服务和持久库可重启恢复，并可用同一 Dockerfile 构建 linux/amd64 与 linux/arm64 镜像。

## 确定性测试场景

1. 拓扑测试：固定三雨水斗管网锁定成功，逐一断言无环、端口唯一及唯一出户可达路径。
2. 拓扑测试：注入回边、悬空端口、双重端口归属和非法管径突变，断言稳定错误码及确定原因顺序。
3. 算术测试：验证整数毫米坐标、坡度和管内容积的舍入边界，并覆盖负值、除零及乘加溢出。
4. 谱系测试：裁切管材为安装短节、试样和损耗，精确断言父级长度守恒及全部唯一身份。
5. 并发事务测试：两个 goroutine 用同步屏障竞争同一管材及端口，仅一个成功，失败事务无谱系边或绑定残留。
6. 租约测试：竞争焊机、夹具和内窥通道，验证逻辑到期、续约、过期操作拒绝及重启后的租约恢复。
7. 热熔状态机测试：完整推进九阶段，并分别断言跳步、重复端口、错误夹具和错误代次不改变有效前缀。
8. 热熔边界测试：使用固定温度与压力序列验证翻边、切换时限、压力积分、曲线缺口和冷却等效时长边界。
9. 设备故障测试：脚本化控制器拒绝、传感器断连、超时、校准过期及格式错误，仅生成确定次数的待重试调用。
10. 污染传播测试：同焊机加热板污染窗口和同材料批次的多个焊口生成去重、排序稳定的复验集合。
11. 支路与水试测试：内壁缩颈、渗漏、支架位移和排水不足按水力连接及共同悬吊支路传播，并验证定点蓄水和排空计算。
12. 重接隔离测试：割除旧焊口后创建新代次，验证割除段去向、谱系守恒、旧证据保留及旧代次迟到回执无效。
13. 幂等测试：同操作号同内容得到相同响应，同操作号不同内容返回幂等冲突且状态不变。
14. 回滚恢复测试：在事务提交点前注入故障并重启，断言无部分身份、租约、阶段或蓄水证据；提交后重启结果完整一致。
15. 复核测试：同一人员重复签署或无资质人员签署被拒，两名不同合格人员独立复核成功。
16. 终局竞争测试：三个 goroutine 同步竞争准入、隔离与取消，精确断言只产生一个终局、至多一个准入凭据且重启后不改变。

## 组件追踪关系

1. 规则目录对应锁定、整数几何、合法突径、拓扑和热熔阈值规则，主要由拓扑测试、算术测试和热熔边界测试覆盖。
2. 任务及水力拓扑聚合对应只追加连接图、端口当前归属、唯一出户路径、任务代次和重接隔离，由拓扑、重接及迟到回执测试覆盖。
3. 材料谱系与资源租约管理器对应身份及长度守恒、原子端口绑定、并发领用和六类互斥资源，由谱系、并发事务、租约和回滚恢复测试覆盖。
4. 热熔轨迹及安装证据记录器对应连续阶段前缀、压力与冷却证据、内窥和设备失败处理，由状态机、边界及脚本化设备故障测试覆盖。
5. 蓄水复验与终局仲裁器对应异常传播、定点水力、复验换代、双人复核和单写终局，由污染传播、支路水试、复核及终局竞争测试覆盖。
6. Go HTTP API 对应稳定错误、Operation-Id 幂等、JSON 命令查询、事务提交和恢复，由幂等、回滚恢复及端到端准入场景覆盖。

## 独特性

本计划聚焦大型交通枢纽金属屋面虹吸雨水系统，将高密度聚乙烯材料毫米级谱系、热熔压力轨迹、只追加焊口代次图、共享加热板污染传播、分区蓄水排空证据与临时溢流导排拆除终局统一为单一质量闭环，与既有项目标题及其结构施工放行对象均不重复。
